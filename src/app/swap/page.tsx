import PageShell from "@/components/PageShell";
import SwapCard from "@/components/SwapCard";

export default function SwapPage() {
  return (
    <PageShell title="Swap" subtitle="AMM swap UI (mock)">
      <SwapCard />
    </PageShell>
  );
}
